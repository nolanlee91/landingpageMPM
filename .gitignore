# This repo = MicroPokerMaster public landing page (github.com/nolanlee91/landingpageMPM).
# It is PUBLIC, so only the landing/ site is tracked here.
# app/, marketing/ and PROJECT/ are managed separately — don't commit them here.

# App source has its own git repo
app/

# Marketing is wired to the shared Hermes engine (folder 11)
marketing/

# Internal strategy / pricing / tasks — keep OUT of the public repo
PROJECT/

# Internal ops runbooks — keep OUT of the public repo
STRIPE-LIVE.md
EMAIL-SETUP.md
MARKETING-PLAN.md

# SECRETS — never commit (this is a PUBLIC repo)
Key stripe.md
recovery-codes-vercel.txt
stripe_backup_code.txt
*secret*
*backup_code*
recovery-codes*
*.key
.env
.env.*

# Dependencies / build artifacts
node_modules/
dist/

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.log
